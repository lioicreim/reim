"use client";

export default function LibraryPage() {
  return (
    <main className="container">
      <div className="card">
        <div className="cardBody" style={{ textAlign: "center", padding: "48px" }}>
          <h2 style={{ fontSize: "24px", fontWeight: 700, color: "var(--text)", marginBottom: "16px" }}>
            업데이트 중입니다
          </h2>
          <p style={{ fontSize: "16px", color: "var(--muted)", lineHeight: 1.6 }}>
            자료실 콘텐츠가 곧 추가될 예정입니다.
          </p>
        </div>
      </div>
    </main>
  );
}
